[project]
name = "ml-learning"
version = "0.1.0"
description = "Learning project for ML, neural networks, RAG and AI agents"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
